<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 30px; border-radius: 8px 8px 0 0; text-align: center; }
        .header h1 { margin: 0; font-size: 24px; }
        .content { background: #f9f9f9; padding: 30px; border-radius: 0 0 8px 8px; }
        .task-details { background: white; padding: 20px; border-left: 4px solid #667eea; margin: 20px 0; border-radius: 4px; }
        .detail-row { margin: 12px 0; }
        .label { font-weight: 600; color: #667eea; }
        .button { display: inline-block; background: #667eea; color: white; padding: 12px 30px; text-decoration: none; border-radius: 6px; margin-top: 20px; }
        .footer { text-align: center; font-size: 12px; color: #999; margin-top: 30px; padding-top: 20px; border-top: 1px solid #eee; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🎯 New Task Assigned</h1>
        </div>
        <div class="content">
            <p>Hi {{assigneeName}},</p>
            <p>{{assignerName}} has assigned you a new task in {{organizationName}}.</p>
            
            <div class="task-details">
                <div class="detail-row">
                    <div class="label">Task:</div>
                    <div>{{taskTitle}}</div>
                </div>
                {{#if taskDescription}}
                <div class="detail-row">
                    <div class="label">Description:</div>
                    <div>{{{taskDescription}}}</div>
                </div>
                {{/if}}
                {{#if priority}}
                <div class="detail-row">
                    <div class="label">Priority:</div>
                    <div>{{priority}}</div>
                </div>
                {{/if}}
                {{#if dueDate}}
                <div class="detail-row">
                    <div class="label">Due Date:</div>
                    <div>{{dueDate}}</div>
                </div>
                {{/if}}
                {{#if estimatedHours}}
                <div class="detail-row">
                    <div class="label">Estimated Time:</div>
                    <div>{{estimatedHours}} hours</div>
                </div>
                {{/if}}
            </div>
            
            <p>Log in to {{organizationName}} to view and manage this task.</p>
            <a href="{{taskUrl}}" class="button">View Task</a>
            
            <div class="footer">
                <p>This is an automated notification from {{organizationName}}. You can manage your notification preferences in your account settings.</p>
            </div>
        </div>
    </div>
</body>
</html>
